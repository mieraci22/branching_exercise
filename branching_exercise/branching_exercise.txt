1. git checkout -b
2. fast forwards can only happen if there have not been commits on the original branch while the new branch is being worked on
3. git checkout
4. git branch -D
5. Merge conflicts occur when Git cannot determine which file/folder to choose when merging since there have been different commits with changes to the same file